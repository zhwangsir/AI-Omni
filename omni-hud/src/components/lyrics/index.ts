/**
 * 歌词组件统一导出（M18）。
 *
 * - LyricsDisplay：同步歌词面板（行/逐字/翻译/偏移/空状态）
 *
 * store 经 src/store/lyricsStore + src/store/lyricsRuntime 导出，不在此重复。
 */
export { LyricsDisplay } from "./LyricsDisplay";
export type { LyricsDisplayProps } from "./LyricsDisplay";
