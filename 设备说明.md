# 集群清单

> 最后更新: 2026-08-10（基于 14:58 CST 真机 SSH 调研：Workstation GPU/服务全量核查、Mac Studio 已部署反推/ASR/音频分离服务、spark 节点角色更新、ComfyUI 超分 fleet 上线）
> 维护者: 设备管家(AI Assistant)
> 用途: 所有项目 AI 和管理会话的单一真相源
> 本文件已分发到所有项目目录,统一命名为 `设备说明.md`
> 🔒 **硬性规则**: 本文档仅供参考，GPU 显存/服务状态/端口监听等必须以真机 SSH 命令输出为准

---

## 0、近期变更记录

### 2026-08-10(真机全量调研 + M5/M6 短剧管线交付 + 超分 fleet 上线)

| 变更项 | 旧状态 | 新状态 | 影响 |
|--------|--------|--------|------|
| Workstation GPU 占用 | 文档记录 GPU0~70GB/GPU2~53GB | **真机实测 GPU0=53.0GB / GPU1=69.8GB / GPU2=8.3GB / GPU3=69.3GB** | GPU2 实际最空闲(89.6GB 余量)，文档严重失真已修正 |
| Mac Studio 角色 | 四台环境已清理，待重新部署 | **studio04 部署 Qwen2.5-VL-72B mlx-vlm :9303；studio01 部署 demucs-mlx :9221；studio02 部署 whisper.cpp :9212** | Mac Studio 已承担反推/ASR/音频分离迁移任务 |
| Workstation ASR/demucs | GPU2 原生运行 | **已迁移至 Mac Studio**（studio02 whisper.cpp / studio01 demucs-mlx），Workstation 实例保留作回退 | GPU2 释放显存，Mac Studio 统一内存优势发挥 |
| MiniMax H3 | 未部署 | **toiv-comfyui-h3.service :8195 上线**（GPU0+GPU2 跨卡，UNet 41.3GB） | 长视频生成能力补齐，散热为硬性瓶颈 |
| LongCat-Video | 未部署 | **comfyui-longcat.service :8197 上线**（GPU2 独立实例，fp8+BlockSwap） | 5 分钟级长视频生成引擎 |
| M6 超分 fleet | 无 | **GPU1/2/3 各起 ComfyUI 超分专用实例 :8261/:8262/:8263** | 3 卡并行 4K 超分，128 帧 52s(≈3× 加速) |
| JoyCaption | 未部署 | **toiv-joycaption.service :9304 上线**（GPU3，bf16 16.7GB） | NSFW 反推专线 |
| SenseVoice | 未部署 | **toiv-sensevoice.service :9211 上线**（GPU2，1.5GB） | 语音情绪/事件标注 |
| spark01 角色 | llama-3.3-70b L4 NSFW | **Omni-Captioner 音乐反推 :8000**（Docker omni_captioner） | L4 NSFW 已退役，spark01 转音乐反推 |
| spark02 LLM | Qwen3.5-MoE-35B FP8 | **qwen3.6-35b-a3b-uncensored-heretic FP8**（模型路径已确认） | served-model-name=qwen3.6-uncensored 别名不变 |
| FlashTalk 端口 | :9000 | **:9004** | 解决端口冲突，真机确认监听 |
| studio03 EXO | 待部署 | ⚠️ EXO 进程未检测到 | 仅系统服务运行，待后续部署 |

### 2026-08-09(DRT 迁移至 core + 四台 Mac Studio 环境清理 + FlashTalk 端口修正)

| 变更项 | 旧状态 | 新状态 | 影响 |
|--------|--------|--------|------|
| core 角色 | ToIV 生产业务机（纯应用层） | **DRT 业务服务器 / ToIV 生产业务机** | DRT 服务栈已迁移并运行 :8080 |
| DRT 服务位置 | Workstation Docker / 真机混合 | **core Docker 栈**（7 容器：api/web/caddy/postgres/redis/minio/backup） | Workstation 释放算力资源，GPU 仅跑生成/数字人 |
| workstation Docker | 7 个 DRT 相关容器运行 | **DRT 容器/镜像/卷/目录全部清理**，`/home/merlin/drt` 已删除 | 释放 ~17GB 空间，workstation 回归纯算力角色 |
| FlashTalk 端口 | :9000 | **:9004** | 解决与 drt-minio-prod :9000 端口冲突，flashtalk.service active |
| Mac Studio 环境 | GLM-5.2/Qwen3-VL/toiv-vlm-mlx 等服务残留 | **四台全部清理**：模型/venv/缓存/EXO 残留删除，仅保留基础服务 | studio01-04 进入干净待机状态（后续已重新部署反推/ASR/音频分离） |
| erp.dgmt.top 反代 | 指向 workstation :8080 | **指向 core Tailscale :8080** | 配置已切，但当前返回 502，cloud→core 待排查 |

### 2026-08-05(文本 LLM 全部切 spark + GPU3 改 FlashTalk/OpenTalking)

| 变更项 | 旧状态 | 新状态 | 影响 |
|--------|--------|--------|------|
| 文本 LLM 路由 | workstation :8000(Nemotron 已移除) | **全部切 spark 集群**:L1→spark02 qwen3.6-35b-a3b-uncensored-heretic FP8(:8000) | workstation GPU3 改为 FlashTalk + OpenTalking |
| workstation GPU3 | Nemotron vLLM(89GB) | **FlashTalk + OpenTalking**(`flashtalk.service` + `opentalking.service`) | 数字人实时对话 + 统一 API，GPU3 专用 |
| pc01/pc02 | 此前 LAN 不可达(:8188/:8193) | **✅ LAN 可达**(:8188/:8193 /system_stats 200),LB 后端恢复健康 | 3 后端(gpu0 + pc01 + pc02)全部可用 |
| ToIV TTS | `toiv-tts.service` failed | **已重启 active**(:9200 /health 200) | 短剧配音/数字人 TTS 恢复可用 |

---

## 一、集群总览

### 设备清单(17 台 + 1 操作终端)

> 数据来源：2026-08-10 14:58 CST 真机 SSH 调研

| # | 设备 | 角色 | LAN IP | Tailscale IP | 系统 | 状态 |
|---|------|------|--------|--------------|------|------|
| 1 | core | DRT 业务服务器 / ToIV 生产业务机 | 192.168.71.47 | 100.77.80.100 | Ubuntu | ✅ toiv-api:8090 / toiv-web:3100 / PG18 / Redis / DRT Docker 栈全 active |
| 2 | workstation | AI 生成算力 + 真机服务 | 192.168.71.127 | 100.68.100.90 | Ubuntu | ✅ 17 个 systemd 服务在线，GPU 总占用 51.5% |
| 3 | spark01 | Omni-Captioner 音乐反推 | 192.168.71.82 | 100.81.235.124 | Ubuntu (DGX Spark) | ✅ Docker omni_captioner :8000，Up 42h |
| 4 | spark02 | L1-L4 文本 LLM 主力 | 192.168.71.84 | 100.86.42.89 | Ubuntu (DGX Spark) | ✅ vLLM qwen3.6-35b-a3b FP8 :8000（⚠️ 内存 121/121GB 接近满载） |
| 5 | studio01 | demucs-mlx 人声分离节点 | 192.168.71.109 | 100.67.43.40 | macOS (M3 Ultra 512GB) | ✅ :9221 监听中（PID 14548） |
| 6 | studio02 | whisper.cpp ASR 节点 | 192.168.71.111 | 100.91.0.121 | macOS (M3 Ultra 512GB) | ✅ :9212 监听中（PID 80590） |
| 7 | studio03 | 单机 MLX 推理节点(待部署) | 192.168.71.112 | 100.115.27.68 | macOS (M3 Ultra 512GB) | ⚠️ EXO 进程未检测到，仅基础服务 |
| 8 | studio04 | Qwen2.5-VL-72B 反推 VLM 节点 | 192.168.71.113 | 100.126.182.23 | macOS (M3 Ultra 512GB) | ✅ :9303 监听中（PID 89569） |
| 9 | openclaw01 | OpenClaw 智能体网关 + 告警微信推送 | 192.168.71.86 | 100.69.0.4 | macOS (Mac mini M2) | ✅ OpenClaw :18789 运行 |
| 10 | openclaw02 | OpenClaw 智能体网关 | 192.168.71.75 | 100.76.35.7 | macOS (Mac mini M2) | ✅ 运行中 |
| 11 | openclaw03 | OpenClaw 智能体网关 | 192.168.71.81 | 100.76.140.121 | macOS (Mac mini M2) | ✅ 运行中 |
| 12 | openclaw04 | OpenClaw 智能体网关 | 192.168.71.85 | 100.91.128.30 | macOS (Mac mini M2) | ✅ 运行中 |
| 13 | pc01 | ComfyUI worker (RTX 5090) | 192.168.71.115 | 100.69.134.27 | Windows 11 | ✅ :8188 LISTENING（PID 31128） |
| 14 | pc02 | ComfyUI worker (RTX 5090) | 192.168.71.114 | 100.107.94.26 | Windows 11 | ✅ :8193 LISTENING（PID 9196） |
| 15 | NAS | SMB 存储 (绿联 DXP8800) | 192.168.71.7 | 100.80.237.96 | UGREEN OS | ✅ 44T / 5.2T 已用 (12%) |
| 16 | cloud | 网关 / 1Panel / frps | 43.119.32.180 | 100.83.78.114 | Linux (阿里云) | ✅ OpenResty :80/:443 + frps :7000/:7500 |
| 17 | MateBook | 操作终端 | — | 100.74.15.34 | macOS | ✅ 本机 |

---

## 二、设备详细说明

### 2.1 core — DRT 业务服务器 / ToIV 生产业务机

**硬件**: Ubuntu / 4×10GbE 网卡 / 915GB 磁盘（已用 55GB，7%）
**角色**: DRT 业务服务器（Docker 栈）+ ToIV 生产业务机（真机应用层）

**当前服务(真机 systemd)**:

| 服务 | 端口 | 状态 | 说明 |
|------|------|------|------|
| toiv-api | :8090 | ✅ running 24h+ | FastAPI/Uvicorn，MainPID 560490，212MB RAM |
| toiv-web | :3100 | ✅ running | Next.js 前端 |
| PostgreSQL 18 | :5432 | ✅ running | ToIV 真机数据库（127.0.0.1） |
| Redis | :6379 | ✅ running | 127.0.0.1 + [::1] |

**当前服务(Docker 栈,仅 DRT，7 容器)**:

| 容器 | 端口 | 状态 | 说明 |
|------|------|------|------|
| drt-api-prod | 3001/tcp | ✅ Up 30h | API 服务 |
| drt-web-prod | 3000/tcp | ✅ Up 30h | Web 前端 |
| drt-caddy | 0.0.0.0:8080->80/tcp | ✅ Up 30h | 反向代理，对外提供 :8080 |
| drt-postgres-prod | 5432/tcp | ✅ Up 30h (healthy) | DRT 专用 PostgreSQL |
| drt-redis-prod | 6379/tcp | ✅ Up 30h | DRT 专用 Redis |
| drt-minio-prod | 9000/tcp | ✅ Up 30h | DRT 对象存储 |
| drt-backup | 5432/tcp | ✅ Up 30h | PostgreSQL 备份 sidecar |

**其他监听端口**: :8100（uvicorn，疑似另一 API 实例）、:3501、:8080、:3389/:3390（远程桌面）

**SSH**: `ssh merlin@192.168.71.47`

> ⚠️ `toiv-api.service` unit file 在磁盘上已被修改，建议执行 `systemctl daemon-reload` 消除警告。

---

### 2.2 Mac Studio(studio01-04)— 推理服务节点

**硬件**: Apple M3 Ultra 32核 / 512GB 统一内存 / 80 核 GPU / Metal 4
**角色**: 已从"空闲待部署"转为承担反推/ASR/音频分离迁移任务

| 设备 | 服务 | 端口 | PID | 状态 | 说明 |
|------|------|------|-----|------|------|
| **studio04** | Qwen2.5-VL-72B-Instruct-4bit mlx-vlm | :9303 | 89569 | ✅ 监听中 | 模型 39GB，常驻内存 ~42GB，首次加载 ~1.5s，单图反推 ~5.4s；SFW 图+全部视频走此；launchd `com.dgmt.toiv-vlm-mlx.plist`（OnDemand=false） |
| **studio01** | demucs-mlx 人声分离 | :9221 | 14548 | ✅ 监听中 | htdemucs 模型，与 PyTorch bit-exact；输出目录 `~/toiv-demucs-mlx/outputs/`；launchd `com.dgmt.toiv-demucs-mlx.plist`（OnDemand=true） |
| **studio02** | whisper.cpp large-v3-turbo ASR | :9212 | 80590 | ✅ 监听中 | OpenAI `/v1/audio/transcriptions` 兼容；JFK 样本 WER/CER=0；launchd `com.dgmt.toiv-whisper-cpp.plist`（OnDemand=true）；另有 :9213 whisper-server |
| **studio03** | — | — | — | ⚠️ 待部署 | EXO 进程未检测到，仅基础服务（ControlCenter/beszel/node_exporter/mihomo） |

**SSH**: `ssh dgmt-studio0X@192.168.71.10X`
**IPv6**: autoconf 会轮换,**必须用 LAN IPv4**
**统一内存余量**: studio01 free 31366891 pages×16KB≈484GB / studio02 free 31428397 pages≈485GB / studio04 free 26208625 pages≈404GB

**视频反推特殊处理**: mlx-vlm 的 video_url 只认本地路径，core 把视频 SFTP 落 `/NAS/toiv/reverse_tmp/`，把 studio04 `~/nas_mnt` 挂载路径发给 MLX，完事删除（开关 `TOIV_REVERSE_VIDEO_MAC_PREFIX`，置空退回 base64）。

---

### 2.3 Mac Mini(openclaw01-04)— OpenClaw 智能体网关

**硬件**: Apple M2 / 24GB 统一内存
**角色**: OpenClaw 智能体(唯一正式部署设备)

| 服务 | 端口 | 状态 | 说明 |
|------|------|------|------|
| OpenClaw gateway | :18789 | ✅ 4 台运行 | launchd 托管,KeepAlive；Node v24.18.0 |
| mihomo | :7890 / :9090 | ✅ 4 台运行 | launchd 托管 |
| Tailscale | - | ✅ 4 台在线 | mesh 网络 |
| wechat-bridge(仅 openclaw01) | :9095 | ✅ 运行 | 告警微信推送 |
| beszel-agent | :45876 | ✅ 4 台运行 | 监控 agent |
| node_exporter | :9100 | ✅ 4 台运行 | 指标采集 |

**SSH**: `ssh dgmt-openclaw0X@192.168.71.XX`
**OpenClaw 版本**: 2026.7.1-2
**身份**: 4 台统一为 **Drita**(全能 AI 助手)

**OpenClaw 配置(4 台一致)**:
- models.providers.qwen36:spark02 :8000 qwen3.6-uncensored(**primary**)
- models.providers.nsfw:spark01（已改为 Omni-Captioner，NSFW fallback 待重新规划）
- memorySearch: Workstation :9302(Qwen3-Embedding-4B)
- tools.profile=full / security=full / ask=off(YOLO 全开)

---

### 2.4 Spark(spark01-02)— LLM / Omni-Captioner

**硬件**: NVIDIA DGX Spark / Grace Blackwell / 128GB 统一内存 ×2 / aarch64
**角色**: spark01 音乐反推；spark02 文本 LLM 主力

| 设备 | 角色 | LAN IP | :8000 | 模型 | 状态 |
|------|------|--------|-------|------|------|
| spark01 | Omni-Captioner 音乐反推 | 192.168.71.82 | ✅ 监听 | Docker `omni_captioner` (vllm-node:latest) | Up 42h |
| spark02 | L1-L4 文本 LLM 主力 | 192.168.71.84 | ✅ 监听 | qwen3.6-35b-a3b-uncensored-heretic FP8 | running |

**SSH**: `ssh dgmt-spark@192.168.71.8X`

**spark02 L1 详情**(2026-08-10 真机确认):
- vLLM 进程 PID 3469234，启动于 8月06日
- 模型路径: `/models/qwen3.6-35b-a3b-uncensored-heretic`
- 启动参数:
  ```
  vllm serve /models/qwen3.6-35b-a3b-uncensored-heretic \
    --served-model-name qwen3.6-uncensored --port 8000 --host 0.0.0.0 \
    --tensor-parallel-size 1 --quantization fp8 \
    --max-model-len 32768 --max-num-seqs 16 \
    --gpu-memory-utilization 0.90 \
    --enable-prefix-caching --enable-chunked-prefill \
    --enable-auto-tool-choice --tool-call-parser step3p5 --trust-remote-code
  ```
- served-model-name: `qwen3.6-uncensored`（别名，下游 core/OpenClaw 零改动）
- ⚠️ **内存高压**: 121GB / 121GB，available 仅 403MB，LLM 长上下文或并发调用可能触发 OOM

**spark01 详情**:
- Docker 容器 `omni_captioner`（镜像 vllm-node:latest）+ `node-exporter`
- 端口 :8000（Omni-Captioner）、:9100（node-exporter）、:9404（dcgm-exporter）

---

### 2.5 Workstation — AI 生成算力 + 真机服务

**硬件**: AMD Ryzen Threadripper PRO 9975WX 32C / 183GB RAM / 4× RTX PRO 6000 Blackwell 96GB / 7.3TB 磁盘（已用 797GB，12%）
**角色**: 全部 AI/算力后端服务（ComfyUI-LB、IndexTTS2、ASR、Embedding、LiveAct、H3、LongCat、FlashTalk、OpenTalking、JoyCaption 等）

#### GPU 实时占用（2026-08-10 14:58 真机 nvidia-smi）

| GPU | 温度 | 功耗 | 总显存占用 | 余量 | 主要进程 |
|---|---|---|---|---|---|
| **GPU0** | 33°C | 6 W | **53.0 / 97.9 GB** | 44.9 GB | ComfyUI :8189（2.9 GB）<br>IndexTTS2 :9200（8.6 GB）<br>MiniMax H3 :8195（41.3 GB） |
| **GPU1** | 33°C | 78 W | **69.8 / 97.9 GB** | 28.1 GB | Qwen3-Embedding-4B :9302（10.0 GB）<br>LiveAct :9400（59.1 GB）<br>ComfyUI 超分 :8261（0.8 GB） |
| **GPU2** | 34°C | 77 W | **8.3 / 97.9 GB** | 89.6 GB | LongCat-Video :8197（1.0 GB）<br>SenseVoice :9211（1.5 GB）<br>AI-Omni ASR :9210（4.3 GB）<br>H3 CLIP/VAE 共享（0.6 GB）<br>ComfyUI 超分 :8262（0.8 GB） |
| **GPU3** | 37°C | 14 W | **69.3 / 97.9 GB** | 28.6 GB | JoyCaption :9304（16.7 GB）<br>FlashTalk（50.7 GB）<br>ComfyUI 超分 :8263（0.8 GB）<br>gnome-remote-desktop + Xwayland |

> GPU 总占用: 201.4 / 391.5 GB（约 51.5%）

#### 运行中的 systemd 服务（17 个）

| 服务 | 端口 | GPU | 状态 | 说明 |
|------|------|-----|------|------|
| `comfyui-gpu0.service` | :8189 | GPU0 | ✅ running | ComfyUI 主实例 |
| `comfyui-lb.service` | :8188 | — | ✅ running | ComfyUI 负载均衡 |
| `comfyui-longcat.service` | :8197 | GPU2 | ✅ running 1d17h | LongCat-Video 独立实例（WanVideoWrapper） |
| `comfyui-upscale-gpu1.service` | :8261 | GPU1 | ✅ running | M6 超分专用（4x-UltraSharp，--cache-lru 2 --disable-smart-memory） |
| `comfyui-upscale-gpu2.service` | :8262 | GPU2 | ✅ running | M6 超分专用 |
| `comfyui-upscale-gpu3.service` | :8263 | GPU3 | ✅ running | M6 超分专用 |
| `flashtalk.service` | :9004 | GPU3 | ✅ running | 数字人实时对话 WebSocket（50.7GB） |
| `opentalking.service` | :4403 | GPU3 | ✅ running | 数字人统一 API |
| `opentalking-tts-shim.service` | :19092 | — | ✅ running | OpenTalking → IndexTTS2 转发 |
| `qwen3-embedding.service` | :9302 | GPU1 | ✅ running | Qwen3-Embedding-4B（10.0GB） |
| `toiv-asr.service` | :9210 | GPU2 | ✅ running | faster-whisper large-v3（保留回退，已迁移 studio02） |
| `toiv-audio-sep.service` | :9220 | GPU2 | ✅ running | Demucs 人声分离（保留回退，已迁移 studio01） |
| `toiv-comfyui-h3.service` | :8195 | GPU0+GPU2 | ✅ running 1d17h | MiniMax H3 视频生成（UNet 41.3GB） |
| `toiv-joycaption.service` | :9304 | GPU3 | ✅ running | JoyCaption Beta One bf16（NSFW 反推，16.7GB） |
| `toiv-liveact.service` | :9400 | GPU1 | ✅ running | LiveAct batch worker（59.1GB） |
| `toiv-sensevoice.service` | :9211 | GPU2 | ✅ running | SenseVoice 语音情绪/事件标注（1.5GB） |
| `toiv-tts.service` | :9200 | GPU0 | ✅ running | IndexTTS2 语音合成（8.6GB） |

#### ComfyUI-LB 后端配置

- 本地 1 后端：:8189（GPU0）
- 远程 2 后端：pc01 :8188 / pc02 :8193
- **GPU1 不跑独立 ComfyUI 后端**（GPU1 跑 LiveAct + Embedding；:8261 为 M6 超分专用实例，不入 LB 池）
- **GPU2 例外**: 跑 LongCat-Video 专用独立实例（:8197，不入 LB 池）；:8262 为 M6 超分专用实例
- **GPU3 不跑通用 ComfyUI**（跑 FlashTalk + OpenTalking + JoyCaption；:8263 为 M6 超分专用实例）
- **M6 超分 fleet**: GPU1/2/3 各一个 upscale-only 实例 :8261/:8262/:8263，仅跑 4x-UltraSharp 帧超分，不入 LB 池；真机冒烟 128 帧 3 卡并行 52s（单卡预估 ~154s，≈3× 加速），温度 38-48°C

#### 存储与挂载

- **根分区**: 7.3T / 797G 已用 / 6.1T 可用（12%）
- **/tmp tmpfs**: 92G / 645M 已用（1%）
- **NAS 挂载**: `/home/merlin/nas_mount` 是挂载点（`//192.168.71.7/NAS` 44T / 5.2T 已用 / 39T 可用，12%）

#### 关键服务路径

| 服务 | 路径 | venv |
|------|------|------|
| ComfyUI | /opt/ComfyUI | /opt/ComfyUI/venv (Python 3.12, torch 2.13.0+cu130) |
| ComfyUI-LB | /opt/ComfyUI/comfyui-lb.py | 同上 |
| IndexTTS2 | /home/merlin/index-tts | /home/merlin/index-tts/.venv (Python 3.11, torch 2.8.0+cu128) |
| Qwen3-Embedding-4B | /home/merlin/models/Qwen3-Embedding-4B | /opt/nemotron-venv |
| AI-Omni ASR | /opt/ai-omni-asr | /opt/ai-omni-asr (Python 3.12, faster-whisper 1.2.1) |
| MiniMax H3 | /home/merlin/ComfyUI-h3-eval | ToIV venv |
| LiveAct | /home/merlin/toiv | ToIV venv |
| FlashTalk | /home/merlin/omnirt/runtimes/flashtalk/cuda | FlashTalk venv |
| OpenTalking | /home/merlin/opentalking | OpenTalking venv |
| JoyCaption | /opt/toiv-joycaption | /opt/toiv-joycaption/venv |
| SenseVoice | /opt/toiv-sensevoice | /opt/toiv-sensevoice/venv |
| LongCat-Video | /home/merlin/ComfyUI-longcat | 独立 venv |

**SSH**:
- Tailscale:`ssh merlin@100.68.100.90`（推荐）
- LAN:`ssh merlin@192.168.71.127`

---

### 2.6 PC(pc01-02)— ComfyUI GPU 节点

**硬件**: RTX 5090 32GB / 128GB RAM / Windows 11

| 设备 | ComfyUI 端口 | PID | 状态 |
|------|-------------|-----|------|
| pc01 | :8188 | 31128 | ✅ LISTENING，NAS 模型已加载，睡眠已禁用 |
| pc02 | :8193 | 9196 | ✅ LISTENING，NAS 模型已加载，睡眠已禁用 |

**SSH**: `ssh home@192.168.71.115`（pc01）/ `ssh w@192.168.71.114`（pc02）

**配置**:
- ComfyUI-LB 第 2/3 后端
- 计划任务 "StartComfyUI" 开机自启
- `extra_model_paths.yaml` 指向 `Z:/Windows/ComfyUI/ComfyUIModel` + `Z:/toiv/comfyui-models`
- Windows 凭据管理器保存 NAS 凭据
- 登录时自动挂载 `Z: \\192.168.71.7\NAS`
- 睡眠已禁用（AC=0），防止空闲后 ComfyUI 被终止

---

### 2.7 NAS — 绿联 DXP8800

**硬件**: 44TB 存储（已用 5.2TB，12%，39T 可用）
**系统**: Linux DXP8800PRO-B5C7 6.12.30+ x86_64
**角色**: SMB 文件共享

| 服务 | 端口 | 状态 |
|------|------|------|
| SMB | :445 | ✅ 运行 |
| Tailscale | - | ✅ (100.80.237.96) |

**SSH**: `ssh dgmt-nas@192.168.71.7`（无 sudo 免密）
**SMB 凭据**: `dgmt-nas / Aki.19950108`
**挂载路径**: Mac Studio `~/NAS` / `~/nas_mnt`（studio04），Workstation `/home/merlin/nas_mount`，PC01/PC02 `Z:`，core `/mnt/toiv-nas`

**关键模型路径**:
- `NAS/Windows/ComfyUI/ComfyUIModel/models` — 主模型库（524GB+）
- `NAS/toiv/comfyui-models` — ToIV 专用模型（~260GB）
- `NAS/toiv/comfyui-models/LongCat-Video` — 美团 LongCat-Video 13.6B（83GB）

---

### 2.8 cloud — 阿里云网关

**硬件**: Linux (阿里云) x86_64
**角色**: 网关 / 1Panel / frps 内网穿透 / OpenResty

| 服务 | 端口 | 状态 |
|------|------|------|
| OpenResty | :80 / :443 | ✅ Web 入口/反向代理 |
| 1Panel | :1212 | ✅ 管理面板 |
| frps | :7000 / :7500 | ✅ 内网穿透 |
| Docker | — | ✅ running |
| beszel | :8090 | ✅ 监控 |

**SSH**: `ssh root@43.119.32.180`（公网 banner 超时仍遗留；**推荐 Tailscale `ssh root@100.83.78.114`**）

---

## 三、服务端口速查表

> 2026-08-10 14:58 CST 真机 ss -tlnp / lsof 验证

### Workstation（192.168.71.127）

| 服务 | 端口 | GPU | 说明 |
|------|------|-----|------|
| ComfyUI LB | :8188 | — | 负载均衡入口 |
| ComfyUI gpu0 | :8189 | GPU0 | 主后端 |
| MiniMax H3 | :8195 | GPU0+GPU2 | 视频生成 |
| LongCat-Video | :8197 | GPU2 | 长视频生成 |
| ComfyUI 超分 fleet | :8261/:8262/:8263 | GPU1/2/3 | M6 并行 4K 超分 |
| IndexTTS2 | :9200 | GPU0 | TTS |
| AI-Omni ASR（回退） | :9210 | GPU2 | faster-whisper |
| SenseVoice | :9211 | GPU2 | 语音情绪标注 |
| Demucs（回退） | :9220 | GPU2 | 人声分离 |
| Qwen3-Embedding | :9302 | GPU1 | Embedding |
| JoyCaption | :9304 | GPU3 | NSFW 反推 |
| LiveAct | :9400 | GPU1 | 数字人直播 |
| OpenTalking | :4403 | GPU3 | 数字人统一 API |
| OpenTalking TTS shim | :19092 | — | IndexTTS2 适配 |
| FlashTalk | :9004 | GPU3 | 数字人 WebSocket |

### Mac Studio

| 服务 | 设备 | IP | 端口 |
|------|------|-----|------|
| mlx-vlm Qwen2.5-VL-72B | studio04 | 192.168.71.113 | :9303 |
| demucs-mlx | studio01 | 192.168.71.109 | :9221 |
| whisper.cpp ASR | studio02 | 192.168.71.111 | :9212 |

### Spark

| 服务 | 设备 | IP | 端口 |
|------|------|-----|------|
| Omni-Captioner | spark01 | 192.168.71.82 | :8000 |
| LLM qwen3.6-uncensored | spark02 | 192.168.71.84 | :8000 |

### Core

| 服务 | 端口 | 说明 |
|------|------|------|
| ToIV API | :8090 | FastAPI/Uvicorn |
| ToIV Web | :3100 | Next.js |
| PostgreSQL | :5432 | ToIV 数据库（127.0.0.1） |
| Redis | :6379 | 缓存（127.0.0.1） |
| DRT Caddy | :8080 | DRT 反代 |

### PC Workers

| 服务 | 设备 | IP | 端口 |
|------|------|-----|------|
| ComfyUI | PC01 | 192.168.71.115 | :8188 |
| ComfyUI | PC02 | 192.168.71.114 | :8193 |

---

## 四、服务依赖矩阵

| 服务 | 运行设备 | 端口 | 依赖 | 影响范围 |
|------|---------|------|------|---------|
| vLLM qwen3.6-uncensored | spark02 | :8000 | GPU 0.90 显存 | OpenClaw 4 MacMini + ToIV L1-L4 + 临时 L2/L3 |
| Omni-Captioner | spark01 | :8000 | Docker vllm-node | 音乐反推链路 |
| OpenClaw gateway | 4 MacMini | :18789 | vLLM :8000 | Agent 执行 |
| mlx-vlm Qwen2.5-VL-72B | studio04 | :9303 | NAS 挂载（视频反推） | 图像/视频反推 |
| whisper.cpp ASR | studio02 | :9212 | 模型文件 | 语音听写 |
| demucs-mlx | studio01 | :9221 | 模型文件 | 人声分离 |
| FlashTalk + OpenTalking | Workstation | :9004 / :4403 | GPU3 ~69GB | 数字人实时对话 + 统一 API |
| ComfyUI-LB | Workstation | :8188 | 后端健康（3 个：gpu0 + pc01 + pc02） | 图像/视频生成 |
| MiniMax H3 | Workstation | :8195 | GPU0+GPU2 | 长视频生成（散热瓶颈） |
| LongCat-Video | Workstation | :8197 | GPU2 | 5 分钟级长视频生成 |
| M6 超分 fleet | Workstation | :8261/:8262/:8263 | GPU1/2/3 | 4K 并行超分 |
| IndexTTS2 | Workstation | :9200 | GPU0 | TTS |
| AI-Omni ASR（回退） | Workstation | :9210 | GPU2 | ASR（主力已迁 studio02） |
| Demucs（回退） | Workstation | :9220 | GPU2 | 音频分离（主力已迁 studio01） |
| SenseVoice | Workstation | :9211 | GPU2 | 语音情绪标注 |
| JoyCaption | Workstation | :9304 | GPU3 | NSFW 反推 |
| LiveAct | Workstation | :9400 | GPU1 | 数字人直播 |
| Qwen3-Embedding-4B | Workstation | :9302 | GPU1 | OpenClaw memorySearch |
| coturn TURN | Workstation | :3478 | 无 | WebRTC 跨网段中继 |
| DRT (api/web/caddy) | core | :8080 | postgres/redis/minio | DRT 生产业务 |
| toiv-api/toiv-web | core | :8090/:3100 | PG+Redis+推理端点 | ToIV 生产业务 |
| NAS SMB | NAS | :445 | 网络 | 模型/数据存储 |
| cloud OpenResty | cloud | :80/:443 | frps | 公网入口 |

---

## 五、关键凭据与挂载

> ⚠️ 这些凭据已多次确认,**禁止再次询问用户**

| 服务 | 用户名 | 密码 | 备注 |
|------|--------|------|------|
| NAS SMB | dgmt-nas | Aki.19950108 | 192.168.71.7,共享名 NAS |
| Tailscale Auth Key | — | tskey-auth-kPM5hHvNGY11CNTRL-UTn8rtRjK8Pfw3riNoGB8Pru71VhdRR9C | 已用于 core 设备授权 |

### NAS 挂载方式

**Linux (Workstation)**: 已配置 fstab 自动挂载到 `/home/merlin/nas_mount`
```bash
# 凭据文件:/root/.smbcredentials
username=dgmt-nas
password=Aki.19950108
```

**Windows (PC01/PC02)**: 用 `cmdkey` + `schtasks` 自动挂载
```cmd
cmdkey /add:192.168.71.7 /user:dgmt-nas /pass:Aki.19950108
net use Z: \\192.168.71.7\NAS /persistent:yes
```

**Mac Studio (studio04)**: LaunchAgent `com.dgmt.nas-mount.plist` 持久化挂载到 `~/nas_mnt`

---

## 六、风险提示与待办事项

### 6.1 当前风险点

1. **spark02 内存满载**: 121GB / 121GB，available 仅 403MB，LLM 长上下文或并发调用可能触发 OOM
2. **studio03 EXO 未运行**: EXO RDMA 集群当前可能只有 studio01/02/04 在线或整体未启动
3. **toiv-api unit file 变更警告**: 建议 `systemctl daemon-reload`
4. **GPU1/GPU3 余量紧张**: GPU1 余 28.1GB、GPU3 余 28.6GB，新增常驻服务前需显存评估
5. **H3 长视频散热瓶颈**: GPU0 满载 ~600W，连续/并发作业会叠加温度，需温度熔断保护（≥85°C 中止）
6. **erp.dgmt.top 502**: core :8080 直连正常，cloud→core Tailscale 路径待查

### 6.2 待办事项

- [ ] pc02/spark02 Tailscale 恢复（不影响当前 LAN 使用）
- [ ] studio03 部署规划（EXO 或独立模型服务）
- [ ] erp.dgmt.top 502 排查（cloud→core Tailscale 路径）
- [ ] Cloud SSH banner 超时排查（HTTPS 正常）
- [ ] 监控系统重新规划（core 原 Docker 监控栈已拆除）
- [ ] spark02 内存高压缓解（考虑降低 gpu-memory-utilization 或启用 swap）

---

## 七、易错点记录

1. **GPU 分配不可随意更改**: GPU3 用于 FlashTalk + OpenTalking + JoyCaption；TTS 在 GPU0；H3 跨 GPU0+GPU2；超分 fleet 在 GPU1/2/3
2. **/tmp tmpfs 吃内存**: 大文件打包应使用 `/var/tmp` 或磁盘目录,避免写入 /tmp
3. **Windows SSH session 隔离**: SMB 映射是 per-session 的,需用 cmdkey + 计划任务实现登录自动挂载
4. **ComfyUI-LB 后端数量**: 当前 3 个后端(gpu0 + pc01 + pc02)，GPU1/2/3 不跑通用 ComfyUI 后端（:8261/:8262/:8263 为超分专用，不入 LB 池）
5. **不要通过 SSH 启动 Windows 长期进程**: 应使用计划任务 + bat 脚本,LogonType=InteractiveToken
6. **禁止重复询问 NAS 密码**: 已记录在第五节
7. **新专用实例接入后必须补 resolve_worker 精确匹配**: 每新增与 pool worker 同机的专用 ComfyUI 实例（如 :8197/:8261-8263），必须同步检查 `apps/api/app/deps.py` 的 resolve_worker 精确匹配分支
8. **文档记录不可直接作为容量/状态结论的依据**: 做容量规划、共卡部署、新增服务前，必须 SSH 真机执行 `nvidia-smi`、`systemctl status`、`ss -tlnp` 等命令，拿当前真机输出说话
9. **H3 长视频散热是硬性瓶颈**: 温度熔断≠模型失败，243/294/345/362 帧冷却后均可成功，但散热窗口不稳定
10. **mlx-vlm 的 video_url 只认本地路径**: 视频反推走 NAS 中转（core SFTP 落 `/NAS/toiv/reverse_tmp/`，把 studio04 `~/nas_mnt` 路径发给 MLX）
